import { ValidateInputDirective } from './directives/validate-input.directive';

describe('ValidateInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateInputDirective();
    expect(directive).toBeTruthy();
  });
});
